export const STATUS_PILL_CLASSES = {
  success:
    "inline-flex items-center rounded-full bg-emerald-600 px-2 py-0.5 text-[10px] font-semibold text-white",

  danger:
    "inline-flex items-center rounded-full bg-red-600 px-2 py-0.5 text-[10px] font-semibold text-white",
} as const;
